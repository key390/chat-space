.chat-side
  .chat-side-header
    .top-items
      %p.top-items__name
        = current_user.name
        %ul.top-items__icon-list
          %li 
          = link_to new_group_path, class: "top-items__icon" do
            = icon('fas', 'edit')
            %span<>

          %li
          = link_to edit_user_path(current_user) , class: "top-items__icon" do
            = icon('fas', 'cog')
            %span<>
            
  .chat-side-groups
    - current_user.groups.each do |group|
      .groups
        = link_to "#"  do
          .groups-name
            = group.name
          .groups-message
            メッセージはまだありません



  


    
    

 